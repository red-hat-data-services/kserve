apiVersion: serving.kserve.io/v1alpha2
kind: LLMInferenceServiceConfig
metadata:
  name: kserve-config-llm-prefill-template-nvidia-cuda
  annotations:
    openshift.io/display-name: vLLM NVIDIA CUDA GPU LLMInferenceServiceConfig (Prefill)
    openshift.io/description: vLLM NVIDIA CUDA GPU LLMInferenceServiceConfig (Prefill) for LLMInferenceService.
    opendatahub.io/recommended-accelerators: '["nvidia.com/gpu"]'
spec:
  prefill:
    template:
      containers:
        - name: main
          image: placeholder
